3.
#include <stdio.h>

3. covert the temperature fahrenhiet into celsius:-

int calc(int);

int main()

{
 
 int f,c;
 
 printf("convert the temperature from fahrenhiet to celsius: ");
  scanf("%d",&f);
 
 c=(f - 32) * 5 / 9;
 
 printf("degree in celsius: %d",c);
 
 }

  int calc(int f)
 
 {
    
  return (f - 32) * 5 / 9;
 
 }

